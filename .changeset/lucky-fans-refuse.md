---
'@loopstack/loopstack-studio': patch
---

Align Studio with the wire-true contracts types: workflow titles are nullable, document timestamps are ISO strings, and the unlock action no longer sends the unused `name` field on its transition payload.
