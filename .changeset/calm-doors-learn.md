---
'@loopstack/cli': patch
---

`loopstack create` scaffolds a `CLAUDE.md` into the new app — project structure, workflow/tool/document conventions, and the CLI feedback loop (`list` → `run` → `runs`), so coding agents start primed.
