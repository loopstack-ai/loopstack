---
'@loopstack/testing-examples': minor
---

First real testing examples: a deterministic ticket-triage workflow with specs demonstrating `testTool()` unit tests, `runWorkflow()` workflow tests with scripted HITL answers, and record/replay from a committed fixture.
