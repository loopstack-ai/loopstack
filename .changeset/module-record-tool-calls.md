---
'@loopstack/loopstack-module': minor
---

New `recordToolCalls` option (or `LOOPSTACK_RECORD_TOOL_CALLS=true`): debug mode that persists every tool call's args and response envelope per run — the recording source for replay fixtures.
