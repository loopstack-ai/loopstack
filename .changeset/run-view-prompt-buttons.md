---
'@loopstack/loopstack-studio': patch
---

Run View prompt buttons use the full width of the prompt box: the confirm Yes/No buttons split the row evenly, and choice options stack full-width instead of wrapping — easier to hit and more consistent across prompt widths.
