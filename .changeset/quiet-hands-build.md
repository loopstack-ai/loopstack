---
'@loopstack/cli': patch
---

Docs for `loopstack create` in the README and CLI reference: the scaffold quickstart (docker compose up, start:dev, first run) and the `--skip-install`/`--no-git` flags.
