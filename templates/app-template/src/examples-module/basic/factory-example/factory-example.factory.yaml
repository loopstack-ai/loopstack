parallel: true                                 # Run all instances in parallel. Defaults to false.
factory:
  block: ProcessItemWorkflow
  args:
    index: ${ this.itemIndex }
    label: ${ this.itemLabel }
iterator:
  source: ${ this.fruits }
  label: ${ item }
namespace:
  label: Fruits