sequence:
  - name: ResearchTopicExampleWorkflow      # Step 1: Research
    assign:
      parent.researchResult: ${ this.researchResult }
  - name: WriteContentExampleWorkflow       # Step 2: Write content
    args:
      researchResult: ${ parent.researchResult }
    assign:
      parent.createdContent: ${ this.createdContent }
  - name: PublishContentExampleWorkflow     # Step 3: Publish
    args:
      createdContent: ${ parent.createdContent }