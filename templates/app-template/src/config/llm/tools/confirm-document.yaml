tools:
  - name: llm_confirmDocument
    execute:
      service: createDocument
      arguments:
        document: ${ workflow.options.document }
        update:
          meta:
            enableTransitions:
              - confirm
              - chat
            enableAtPlaces:
              - document-created
          content: ${ workflow.payload ?? DOCUMENT_DRAFT }