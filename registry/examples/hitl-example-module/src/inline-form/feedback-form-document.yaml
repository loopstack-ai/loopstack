widget: form
options:
  properties:
    rating:
      title: Rating (1–5)
      widget: number
    comment:
      title: Comment
      widget: textarea
  actions:
    - type: button
      transition: submitFeedback
      label: Submit Feedback
