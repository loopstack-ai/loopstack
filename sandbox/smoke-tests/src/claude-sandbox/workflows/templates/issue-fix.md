You are working inside a freshly cloned git repository at /workspace.
Implement a fix for the following GitHub issue. Make the necessary code changes directly in the
working tree. Do NOT run git commit, git push, or open a pull request — that is handled for you
afterwards. When finished, briefly summarize what you changed and why.

Issue #{{issueNumber}}: {{{title}}}

{{{body}}}
