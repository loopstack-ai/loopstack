{
  "name": "acceptance-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --reporter=json --outputFile=results.json",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@loopstack/client": "^0.37.0",
    "vitest": "^3.2.3"
  }
}
