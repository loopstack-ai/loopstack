{
  "name": "acceptance-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --reporter=json --outputFile=results.json",
    "test:watch": "vitest"
  },
  "dependencies": {
    "vitest": "^3.2.3"
  }
}
