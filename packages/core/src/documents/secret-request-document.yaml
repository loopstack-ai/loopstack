type: document
ui:
  form:
    properties:
      variables:
        title: Secrets
        fixed: true
        items:
          order:
            - key
            - value
          properties:
            key:
              title: Key
              readonly: true
            value:
              title: Value
  actions:
    - type: custom
      widget: secret-input
      options:
        transition: secrets_submitted
        label: Save & Continue
